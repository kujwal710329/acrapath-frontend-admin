// All job-post metadata (job categories, roles, skills, fields of study, etc.)
// is now served from the metadata API via useMetadataData().
// See: hooks/useMetadataData.js → services/metadata.service.js → GET /metadata/all
